services:
  - type: web
    name: thought-bot
    env: python
    plan: starter  # או standard לפי הצורך
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: MONGODB_URI
        sync: false
